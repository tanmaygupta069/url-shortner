/**
 *
 * @param {String} url original url to shorten
 * @returns short url
 */
const decodeUrl = (url) => {
  return {
    result: "",
    error: null,
  };
};

module.exports = decodeUrl;
